# https://taskfile.dev

version: "3"

dotenv: [".env"]

tasks:
  default:
    cmds:
      - task --list-all

  build:
    cmds:
      - cargo build

  run:
    cmds:
      - cargo run

  format:
    cmds:
      - cargo fmt --all

  test:
    cmds:
      - cargo test

  coverage:
    env:
      RUSTFLAGS: "-C instrument-coverage"
    cmds:
      - cargo test

  lint:
    cmds:
      - cargo clippy

  install:
    cmds:
      - cargo fetch

  db:migrate:
    desc: "Run sql migration files against database"
    cmds:
      - sqlx migrate run
